/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.nicepng.com', "clipart-library.com", "assets.myntassets.com", "icons8.com"], // Add your domain(s) here
    },
}

module.exports = nextConfig
