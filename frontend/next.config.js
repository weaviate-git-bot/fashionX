/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.nicepng.com', "clipart-library.com"], // Add your domain(s) here
    },
}

module.exports = nextConfig
